$(document).ready(function() {

    // display the date in the header using the id and copied the format directly from moment.js
    $("#currentDay").text(moment().format('MMMM Do YYYY, h:mm:ss a'));
    
    //create a div to hold all the time blocks
    // var timeBlock = $("<div>");
    // timeBlock.addClass("time-block");
    // console.log("time block")

    // // array to hold hour options
    // var hourOptions = [9, 10, 11, 12, 1, 2, 3, 4, 5];

    // for (var i = 0; i<hourOptions.length; i++){

    //     //create time div
    //     //create text area 
    //     //create save btn
    // }
});

